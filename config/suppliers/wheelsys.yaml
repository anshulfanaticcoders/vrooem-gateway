id: wheelsys
name: Wheelsys
enabled: true
protocol: rest_json
auth_type: api_key
supports_one_way: false
default_currency: EUR
timeout: 30
countries: ["GR"]
notes: "REST JSON API. API key auth. Prices returned in cents. Greece."
